Voici comment installer Fish  et le définir comme shell par défaut.

`sudo pacman -S fish` Archlinux

`sudo apt install fish` Ubuntu ou Débian

`sudo eopkg it fish` Solus

et le mettre par défault on fait la commande suivante :

`chsh -s /usr/bin/fish`
